# bootstrap-ci

Companion to **Course 1, Week 3, Session 2 — Bootstrap and permutation.**
Choose a statistic, a population, a sample size, and a number of
bootstrap replications. The app draws one sample, then resamples it B
times to give a bootstrap distribution and a percentile CI.

Packages: `shiny`, `ggplot2`.
